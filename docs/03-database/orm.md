# ORM-описание

## Основные сущности
- `User`
- `Profile`
- `Meal`
- `WorkoutPlan`
- `ProgressEntry`

## Принципы отображения
- одна таблица соответствует одной основной сущности;
- связи оформлены через `@OneToOne` и `@OneToMany`;
- ключи и ограничения задаются на уровне БД и ORM.
